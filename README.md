POEHALI TEAM — Vercel + Neon

1. DATABASE_URL уже должна быть добавлена в Vercel.
2. Добавьте в Vercel Environment Variables ещё одну переменную APP_SECRET со случайной длинной строкой (30+ символов).
3. Загрузите ВСЕ файлы этой папки в корень GitHub-репозитория, заменив старые.
4. Vercel автоматически выполнит deployment.
5. Первый PIN управляющего: 2026. Сразу смените его в Настройки.

Фото сотрудников и новостей хранятся в PostgreSQL как data URL. Для этой небольшой внутренней системы это позволяет не подключать отдельное платное файловое хранилище. Ограничение интерфейса: до 900 КБ на фото.

V3: добавлен конкурс «Гонка экипажей». Новые таблицы создаются автоматически; существующие данные Neon не удаляются.

V4: анимации переходов, салют при входе сотрудника и при положительном начислении баллов. База данных не изменяется.

V5: улучшенные переходы, индивидуальные задания, роли доступа employee/supervisor, флаг участия в командном рейтинге. Миграции только добавляют поля/таблицу и не удаляют текущие данные.

V5.1 HOTFIX: исправлена синтаксическая ошибка frontend JavaScript. База данных и схема не изменяются.

V6:
- активные индивидуальные задания отображаются сразу на главной сотрудника;
- Web Push уведомления о новых индивидуальных заданиях;
- добавлен service worker /sw.js;
- требуется VAPID_PUBLIC_KEY и VAPID_PRIVATE_KEY в Vercel Environment Variables;
- текущие данные Neon не удаляются.

V7: полный визуальный редизайн в современной премиальной стилистике фирменного логотипа. Backend и Neon-схема не изменяются.

V7.1: подзаголовок «Новости, конкурсы и достижения»; более мягкие переходы между вкладками.

V7.2: полностью обновлён экран входа/выбора сотрудника; улучшены сообщения диагностики push. Для push по-прежнему обязательны VAPID_PUBLIC_KEY и VAPID_PRIVATE_KEY в Vercel.

V7.3:
- мобильная горизонтальная лента вкладок сохраняет позицию после переключения;
- push diagnostics: /api/push/status и /api/push/test;
- кнопка тестового уведомления;
- ожидание serviceWorker.ready перед подпиской;
- manifest.webmanifest и PWA metadata для мобильных устройств;
- service worker сразу активируется через skipWaiting/clients.claim.

V7.4:
- push на индивидуальные задания;
- push всей активной команде на новые новости;
- push всей активной команде на новые общие задания;
- push всей активной команде при запуске/обновлении активного конкурса;
- deep-link уведомлений сразу в Новости / Задания / Конкурс;
- декоративный красный круг на экране входа перенесён в фон за текстом.

V7.4.1 HOTFIX: исправление BadJwtToken; новый VAPID subject, автоматическая проверка applicationServerKey и переподписка устройства при смене ключей.

V8 Messenger:
- закрытый внутренний мессенджер по отдельному разрешению управляющего;
- личные и групповые чаты;
- текст и изображения;
- ответы на сообщения;
- реакции 👍 ❤️ 🔥;
- редактирование/удаление собственных сообщений;
- непрочитанные сообщения и отметка прочтения;
- индикатор набора текста и онлайн-статус;
- поиск по чатам;
- push-уведомления о новых сообщениях;
- polling ~3.5 сек, без отдельного WebSocket-сервера;
- миграции только добавляют поля/таблицы, существующие данные Neon не удаляются.

V8.1 Interface Editor:
- таблица ui_texts в Neon;
- управляющий получает вкладку «Редактор интерфейса»;
- редактирование бренда, экрана входа, названий вкладок, главной сотрудника, названий разделов, профиля, мессенджера и вкладок управляющего;
- изменения применяются глобально без изменения кода и сохраняются между deployment;
- можно вернуть любой текст к стандартному значению.

V8.2 Full Text Editor:
- добавлена таблица ui_replacements;
- управляющий может создавать глобальные замены любой точной фразы на сайте;
- замены применяются к текстовым узлам, placeholder и title;
- MutationObserver автоматически применяет замены к динамически отрисованным экранам;
- стандартный редактор полей сохранён и дополнен поиском;
- глобальные замены сохраняются в Neon и не теряются после deployment.
