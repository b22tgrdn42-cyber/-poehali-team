POEHALI TEAM — Vercel + Neon

1. DATABASE_URL уже должна быть добавлена в Vercel.
2. Добавьте в Vercel Environment Variables ещё одну переменную APP_SECRET со случайной длинной строкой (30+ символов).
3. Загрузите ВСЕ файлы этой папки в корень GitHub-репозитория, заменив старые.
4. Vercel автоматически выполнит deployment.
5. Первый PIN управляющего: 2026. Сразу смените его в Настройки.

Фото сотрудников и новостей хранятся в PostgreSQL как data URL. Для этой небольшой внутренней системы это позволяет не подключать отдельное платное файловое хранилище. Ограничение интерфейса: до 900 КБ на фото.

V3: добавлен конкурс «Гонка экипажей». Новые таблицы создаются автоматически; существующие данные Neon не удаляются.

V4: анимации переходов, салют при входе сотрудника и при положительном начислении баллов. База данных не изменяется.

V5: улучшенные переходы, индивидуальные задания, роли доступа employee/supervisor, флаг участия в командном рейтинге. Миграции только добавляют поля/таблицу и не удаляют текущие данные.

V5.1 HOTFIX: исправлена синтаксическая ошибка frontend JavaScript. База данных и схема не изменяются.

V6:
- активные индивидуальные задания отображаются сразу на главной сотрудника;
- Web Push уведомления о новых индивидуальных заданиях;
- добавлен service worker /sw.js;
- требуется VAPID_PUBLIC_KEY и VAPID_PRIVATE_KEY в Vercel Environment Variables;
- текущие данные Neon не удаляются.

V7: полный визуальный редизайн в современной премиальной стилистике фирменного логотипа. Backend и Neon-схема не изменяются.

V7.1: подзаголовок «Новости, конкурсы и достижения»; более мягкие переходы между вкладками.

V7.2: полностью обновлён экран входа/выбора сотрудника; улучшены сообщения диагностики push. Для push по-прежнему обязательны VAPID_PUBLIC_KEY и VAPID_PRIVATE_KEY в Vercel.

V7.3:
- мобильная горизонтальная лента вкладок сохраняет позицию после переключения;
- push diagnostics: /api/push/status и /api/push/test;
- кнопка тестового уведомления;
- ожидание serviceWorker.ready перед подпиской;
- manifest.webmanifest и PWA metadata для мобильных устройств;
- service worker сразу активируется через skipWaiting/clients.claim.

V7.4:
- push на индивидуальные задания;
- push всей активной команде на новые новости;
- push всей активной команде на новые общие задания;
- push всей активной команде при запуске/обновлении активного конкурса;
- deep-link уведомлений сразу в Новости / Задания / Конкурс;
- декоративный красный круг на экране входа перенесён в фон за текстом.

V7.4.1 HOTFIX: исправление BadJwtToken; новый VAPID subject, автоматическая проверка applicationServerKey и переподписка устройства при смене ключей.

V8 Messenger:
- закрытый внутренний мессенджер по отдельному разрешению управляющего;
- личные и групповые чаты;
- текст и изображения;
- ответы на сообщения;
- реакции 👍 ❤️ 🔥;
- редактирование/удаление собственных сообщений;
- непрочитанные сообщения и отметка прочтения;
- индикатор набора текста и онлайн-статус;
- поиск по чатам;
- push-уведомления о новых сообщениях;
- polling ~3.5 сек, без отдельного WebSocket-сервера;
- миграции только добавляют поля/таблицы, существующие данные Neon не удаляются.

V8.1 Interface Editor:
- таблица ui_texts в Neon;
- управляющий получает вкладку «Редактор интерфейса»;
- редактирование бренда, экрана входа, названий вкладок, главной сотрудника, названий разделов, профиля, мессенджера и вкладок управляющего;
- изменения применяются глобально без изменения кода и сохраняются между deployment;
- можно вернуть любой текст к стандартному значению.

V8.2 Full Text Editor:
- добавлена таблица ui_replacements;
- управляющий может создавать глобальные замены любой точной фразы на сайте;
- замены применяются к текстовым узлам, placeholder и title;
- MutationObserver автоматически применяет замены к динамически отрисованным экранам;
- стандартный редактор полей сохранён и дополнен поиском;
- глобальные замены сохраняются в Neon и не теряются после deployment.

V8.2.1 HOTFIX: исправлены синтаксические ошибки frontend JS после внедрения редактируемых текстов; проверено node --check. База Neon не изменяется.

V8.3 Block Manager:
- таблица ui_blocks в Neon;
- управляющий может скрывать/возвращать крупные окна и целые разделы интерфейса;
- скрытие не удаляет данные и обратимо;
- отдельная кнопка «Вернуть все окна»;
- настройки сохраняются после deployment.

V8.4 Messenger Admin:
- Telegram-подобный внутренний мессенджер сохранён;
- отдельный доступ messenger_access для каждого сотрудника;
- без доступа вкладка «Чаты» не отображается и API запрещает переписку;
- управляющий получил отдельное окно «Мессенджер»;
- можно включать/выключать доступ сотрудникам;
- при отключении сотрудник удаляется из всех чатов;
- управляющий может удалить чат целиком.

V8.5:
- обязательный onboarding сотрудника после PIN;
- экран «Добро пожаловать в команду!» с анимацией и салютом;
- обязательное включение push перед переходом в кабинет на первом устройстве;
- точечный редактор интерфейса: удаление конкретной кнопки по её тексту;
- точечный редактор интерфейса: удаление конкретного окна/карточки по заголовку;
- правила удаления сохраняются в Neon и обратимы.

V8.5.1 Messenger access save fix:
- галочки доступа к мессенджеру теперь редактируются локально;
- добавлена явная кнопка «Сохранить доступ сотрудников»;
- при сохранении все значения отправляются на сервер;
- после сохранения список перечитывается из базы, чтобы подтвердить фактическое состояние;
- сотрудник с сохранённым messenger_access получает вкладку «Чаты» после обновления/повторного входа.

V8.5.2 HOTFIX:
- добавлен отсутствовавший POST /api/admin/messenger/access;
- сохранение галочек доступа теперь реально записывает messenger_access в employees;
- API после записи перечитывает сохранённое значение из Neon;
- отключение доступа не удаляет историю чатов и сообщения;
- frontend и backend проверены node --check.

V8.5.4:
- запрос уведомлений убран из экрана после PIN;
- после каждого входа сотрудника показывается «Добро пожаловать в команду!» с салютом и автоматическим переходом;
- запрос разрешения на push остаётся только на главной странице;
- после успешной подписки окно push автоматически исчезает;
- если устройство уже подписано, push-карточка удаляется сразу после проверки статуса.

V8.6: новые должности и серверная матрица прав сотрудников на задания, новости, конкурсы, призы, достижения и выдачу прав.

V8.7:
- полноценное редактирование карточек сотрудников из кабинета управляющего;
- редактируются имя, должность, роль, PIN, участие в рейтинге, мессенджер и служебные права;
- служебные права сохраняются через admin/employees PUT;
- обновлён современный дизайн input/select/textarea/checkbox;
- карточки сотрудников получили современную редактируемую компоновку.

V8.8 Optional Images:
- необязательные фото для общих заданий;
- необязательные фото для индивидуальных заданий;
- необязательные фото для призов;
- новости сохраняют фото как необязательное поле;
- фото доступны управляющему и сотрудникам с соответствующими правами;
- если файл не выбран, создание работает как раньше.

V8.8.1:
- изображения больше не встраиваются внутрь карточек заданий и призов;
- фото выводится отдельным визуальным элементом соответствующего пункта раздела;
- текстовая карточка остаётся самостоятельной и располагается под изображением;
- отсутствие фото не меняет отображение и функциональность.

V8.9 Unified Manager:
- отдельный PIN и отдельный вход управляющего удалены;
- сотрудник с должностью «Управляющий» входит как обычный сотрудник по персональному PIN;
- его личный кабинет и административный функционал объединены;
- Управляющий получает полный набор прав на контент, сотрудников, мессенджер, интерфейс и настройки;
- legacy manager login отключён;
- старые manager-токены принудительно возвращаются на обычный экран входа.

V8.9.1 HOTFIX:
- исправлен ReferenceError employeeManager при входе сотрудника «Управляющий»;
- employeeManager теперь вычисляется сразу после авторизации и до admin/supervisor маршрутов;
- объединённый кабинет управляющего сохранён;
- отдельный PIN управляющего остаётся отключён.

V8.10:
- у сотрудника «Управляющий» основная горизонтальная навигация приведена к обычному сотруднику;
- административные разделы убраны из основной плашки;
- справа добавлена вкладка «Управление»;
- при наведении или фокусе раскрывается выпадающее меню со всеми административными разделами;
- активный административный пункт подсвечивается.

V8.11 PWA hint:
- на главной сотрудника добавлена подсказка по установке сайта на домашний экран;
- инструкции отдельно для iPhone/Safari и Android/Chrome;
- при наличии beforeinstallprompt показывается системная кнопка «Установить приложение»;
- при событии appinstalled карточка удаляется;
- при запуске сайта в standalone/PWA режиме карточка не отображается;
- та же логика применяется к главной сотрудника «Управляющий».

V8.11.1:
- выпадающее меню «Управление» теперь открывается по тапу/клику;
- закрывается при тапе/клике по любой области вне меню;
- закрывается после выбора административного пункта;
- закрывается клавишей Escape;
- сохранено открытие по hover на компьютере.

V8.12 Mobile hamburger:
- на мобильной версии слева в шапке появляется кнопка с тремя полосками;
- стандартная горизонтальная навигация скрывается;
- по нажатию открывается боковое меню со всеми разделами;
- для Управляющего отдельно показываются обычные разделы и административные пункты;
- меню закрывается по тапу вне панели, по кнопке × и по Escape.

V8.12.1:
- мобильная кнопка меню полностью скрыта до успешной авторизации сотрудника;
- после входа по PIN кнопка появляется в кабинете;
- при выходе снова скрывается;
- цвет кнопки изменён с чёрного на фирменный красно-бордовый градиент;
- сохранено боковое мобильное меню со всеми доступными разделами.

V8.13:
- все доступные сотруднику новости отображаются непосредственно на главной странице;
- новости выводятся полным списком, а не только закреплённые;
- сохранена отдельная вкладка «Новости»;
- аналогичный новостной блок добавлен на главную сотрудника «Управляющий»;
- фото новости, категория, дата и прочие элементы используют существующий newsCard.

V8.13.1:
- исправлено мобильное боковое меню сотрудника «Управляющий»;
- меню теперь корректно читает профиль из state.personal.employee;
- в левой плашке снова отображаются обычные разделы и отдельный блок «Управление»;
- переходы по пунктам мобильного меню работают с unifiedManager.

V8.14:
- управляющий может создавать личные и общие чаты из административного раздела;
- управляющий может удалять любой чат;
- все сотрудники с доступом к мессенджеру могут добавлять сотрудников в групповой чат;
- сотрудники могут покидать чат;
- групповой чат можно переименовать;
- исправлен desktop hover для меню «Управление»: выпадающий список не исчезает при переводе мыши с кнопки на пункты.

V8.15 Typography:
- единый более аккуратный UI-шрифт по всему сайту;
- увеличены межбуквенные интервалы и высота строки;
- улучшены отступы между заголовками, текстом, кнопками, символами и полями;
- кнопки, плашки, меню, мессенджер и формы получили более свободную типографику;
- мобильная версия также оптимизирована по высоте строк и размерам элементов.

V8.16 Mobile spacing redesign:
- значительно увеличены вертикальные отступы между карточками, полями и кнопками на мобильных;
- формы переведены в один столбец с увеличенным gap;
- кнопки стали выше и свободнее;
- панели прав, карточки сотрудников, меню и мессенджер получили отдельные мобильные отступы;
- мобильное меню и dropdown управления стали просторнее;
- на экранах до 420px основные кнопки растягиваются на ширину для удобства.

V8.16.1:
- увеличен отступ вокруг плашек категорий новостей, включая «Для команды»;
- на мобильной версии между плашкой, заголовком/текстом новости теперь больше воздуха;
- остальная мобильная компоновка V8.16 сохранена.

V8.17 Dynamic transitions:
- плавный выход старого раздела и появление нового;
- каскадное появление карточек;
- spring-анимации активных вкладок и кнопок;
- улучшенная анимация выпадающего меню «Управление»;
- более выразительное открытие мобильного бокового меню;
- лёгкие hover/press эффекты;
- shimmer-эффект на основных кнопках;
- поддержка prefers-reduced-motion.

V8.18 Profile photo crop:
- исходное фото профиля можно выбирать размером до 12 МБ;
- перед загрузкой открывается встроенный квадратный cropper;
- фото можно перемещать мышью/пальцем и масштабировать;
- результат сохраняется как JPEG 1024x1024;
- изображение автоматически оптимизируется перед отправкой;
- серверный лимит сохранённого фото увеличен примерно до 3 МБ.

V8.19 Team birthdays:
- раздел «Команда» доступен всем сотрудникам;
- сотрудники могут смотреть фотографии профиля и должности коллег;
- в карточке сотрудника управляющий может указать дату рождения;
- дата рождения отображается коллегам без года;
- ежедневный Vercel Cron проверяет дни рождения;
- за 7 дней и за 1 день всем push-подписчикам отправляется напоминание;
- birthday_notifications предотвращает повторную отправку одного и того же напоминания в одном году.

V8.19.1 HOTFIX:
- исправлен endpoint сохранения обрезанного фото: profile/photo -> me/photo;
- после сохранения профиль перечитывается с сервера;
- корректное обновление работает и для обычного сотрудника, и для сотрудника «Управляющий»;
- input файла очищается после успешного сохранения.

V8.19.2:
- плашка категории новости («Для команды», «Важно» и т.д.) выровнена по одной горизонтальной линии с заголовком;
- увеличен аккуратный gap между плашкой и названием;
- одинаковое оформление в пользовательской ленте и кабинете управляющего.

V8.19.3 FINAL PROFILE PHOTO FIX:
- обычный профиль сотрудника переведён со старого file64 (лимит 900 КБ) на cropper;
- управляющий и обычные сотрудники используют один механизм загрузки;
- исходник до 12 МБ;
- квадратная обрезка и оптимизация до отправки;
- сохранение через /api/me/photo;
- лимит обработанного изображения на backend увеличен до 4 МБ;
- после сохранения профиль перечитывается с сервера.

V8.19.4 PROFILE CROP BLACK SQUARE FIX:
- полностью переработана генерация обрезанного изображения;
- координаты crop переводятся в натуральные пиксели исходного фото и жёстко ограничиваются его границами;
- исключены пустые области canvas, которые могли давать чёрный квадрат;
- JPEG создаётся через canvas.toBlob вместо toDataURL;
- перед отрисовкой canvas заполняется белым фоном;
- результат 768x768, качество 0.88;
- добавлена проверка на подозрительно пустой результат;
- затемнение crop preview больше не перекрывает всю фотографию.

V8.19.5 CANVAS CROPPER:
- предпросмотр фото больше не использует DOM <img>;
- файл декодируется через createImageBitmap с fallback на Image;
- crop preview полностью рисуется на canvas;
- корректно учитывается ориентация изображения;
- масштабирование и перетаскивание выполняются прямо на canvas;
- экспорт использует тот же декодированный source;
- добавлена проверка на фактически пустой/чёрный результат;
- для неподдерживаемого HEIC/HEIF выводится понятное сообщение.

V8.20 Smart Date Inputs:
- даты вводятся вручную в формате ДД.ММ.ГГГГ;
- можно печатать только цифры: 16081995 автоматически превращается в 16.08.1995;
- проверяется существование даты;
- некорректное поле подсвечивается;
- перед API-запросом дата автоматически переводится в YYYY-MM-DD;
- существующие даты из базы показываются пользователю как ДД.ММ.ГГГГ;
- применено к дням рождения, срокам индивидуальных заданий, датам конкурсов и датам мероприятий.

V8.20.1:
- логотип в левом верхнем углу стал кнопкой «На главную»;
- работает для обычных сотрудников и сотрудника «Управляющий»;
- используется существующая плавная анимация перехода между разделами;
- добавлены hover/press эффекты;
- на экране ввода PIN логотип не позволяет обойти авторизацию.

V9.0 STABILITY RELEASE
- frontend разделён на index.html + assets/app.css + assets/app.js;
- понятные сетевые/API ошибки;
- журнал действий управляющего;
- мягкое удаление важных сущностей в архив + восстановление;
- ручные и ежедневные логические backup-снимки в Neon;
- экспорт backup в JSON;
- диагностика БД, push, Service Worker, Cron, версии и количества подписок;
- версия приложения 9.0.0 и баннер принудительного обновления при рассинхронизации;
- Service Worker versioning/cleanup;
- автоматическое удаление 404/410 push-подписок при доставке уже сохранено;
- Cron maintenance + birthday status;
- VAPID subject больше не привязан к production URL;
- smoke tests через npm test / npm run check;
- staging-friendly environment отображается в диагностике.

ВАЖНО: backup_snapshots — логические снимки внутри той же Neon-базы. Это защищает от случайного удаления и позволяет скачать JSON, но не заменяет независимый backup провайдера базы данных.

V9.0.1:
- исправлена кнопка логотипа в объединённом кабинете управляющего;
- из любого пункта «Управление» логотип теперь принудительно переводит umtab на home;
- переход больше не зависит от текущей формы state;
- сохранена плавная анимация перехода.

V9.0.2 PUSH DELIVERY FIX
- любая созданная новость отправляет push на каждую активную push-подписку команды;
- любое созданное общее задание отправляет push на каждую активную push-подписку команды;
- индивидуальное задание отправляет push только подпискам назначенного сотрудника;
- одинаковая логика действует для административного кабинета и делегированной панели сотрудников;
- broadcastPush теперь работает напрямую по push_subscriptions, а не циклом по сотрудникам;
- отправки выполняются параллельно;
- 404/410 подписки удаляются автоматически;
- VapidPkHashMismatch / BadJwtToken диагностируются отдельно;
- при каждом входе сайт автоматически проверяет VAPID public key и переподписывает устройство, если ключ подписки устарел;
- результат последнего push сохраняется в system_status и отображается в диагностике.

V9.0.3 EMPLOYEE GENDER:
- в employees добавлено поле gender;
- при создании и редактировании сотрудника управляющий выбирает Мужской / Женский;
- пол отображается в карточке сотрудника;
- персональные формулировки интерфейса учитывают пол;
- «Ознакомился» / «Ознакомилась»;
- «Выполнил» / «Выполнила» для персональных действий;
- для старых сотрудников без выбранного пола используется мужской вариант до редактирования карточки.

V9.0.4 GENDER SAVE HOTFIX:
- исправлен PUT /api/admin/employees: gender теперь реально записывается в Neon;
- admin/state и остальные employee queries возвращают gender;
- новые сотрудники создаются сразу с выбранным полом;
- после сохранения backend перечитывает сотрудника и возвращает фактически сохранённый gender;
- UI подтверждает сохранённое значение и перечитывает карточки.

V9.0.5 GENDER PERSISTENCE FIX:
- generic PUT сотрудника больше не сбрасывает gender в male, если поле не передано;
- добавлен отдельный POST /api/admin/employees/gender для явного сохранения пола;
- интерфейс сначала сохраняет пол отдельным запросом, затем карточку целиком;
- после сохранения оба ответа сверяются с выбранным значением;
- toggle active теперь также передаёт существующий gender.

V9.0.6 LOGIN GENDER FIX:
- стартовый список сотрудников перед вводом PIN теперь получает gender из Neon;
- стартовый экран больше не подставляет «Мужской» при отсутствии/непрочитанном gender;
- отображение пола использует только фактически сохранённое значение male/female;
- employee выбор получил data-gender для диагностики;
- карточки после входа и стартовая авторизация используют одну и ту же модель пола.

V9.0.7:
- пол сотрудников больше не отображается на стартовой странице выбора сотрудника / ввода PIN;
- gender продолжает загружаться и использоваться внутри системы для корректных склонений;
- карточки управляющего по-прежнему позволяют редактировать пол сотрудника.

V9.0.8 LOGO HOME HARD FIX:
- логотип больше не определяет управляющего по текущему state административного раздела;
- при клике заново вызывается /api/me;
- если должность «Управляющий», заново загружается /api/admin/state;
- state формируется заново, umtab принудительно становится home;
- главная отрисовывается напрямую, без отложенного transition callback;
- сбрасываются зависшие классы section-leaving/section-entering;
- работает из любого административного пункта «Управление».

V9.0.9 FAST TRANSITIONS:
- убраны тяжёлые blur-анимации и длинные паузы между разделами;
- fallback задержка уменьшена со 150 мс до 70 мс;
- добавлен нативный View Transition API на поддерживаемых браузерах;
- переходы используют GPU-friendly opacity/translate/scale;
- каскадная анимация применяется только к первым 8 крупным блокам;
- добавлена быстрая фирменная красно-золотая линия перехода;
- логотип у управляющего использует fast path без повторного запроса /api/me и /api/admin/state, если данные уже загружены;
- сохранена поддержка prefers-reduced-motion.

V9.1.0 CONTEXT CREATE:
- сотрудники с разрешением на создание контента получают кнопку «+» прямо в соответствующем разделе;
- Новости → создать новость;
- Задания → общее или индивидуальное задание в зависимости от прав;
- Призы → создать приз;
- Конкурс → сохранить конкурс и добавить конкурсное задание;
- Управляющий получает те же быстрые действия в обычных вкладках сотрудника;
- после создания данные перечитываются и раздел обновляется;
- существующий раздел «Управление» сохранён для расширенного администрирования.

V9.1.1:
- в диагностике управляющего добавлена кнопка «Сбросить все Push-подписки»;
- действие требует двойного подтверждения;
- DELETE /api/admin/push/subscriptions удаляет все записи push_subscriptions;
- количество удалённых подписок возвращается интерфейсу;
- операция записывается в журнал действий и system_status.

V9.1.2:
- кнопка «Сбросить все Push-подписки» теперь реально отображается в «Управление → Диагностика»;
- кнопка находится в отдельном блоке «Push-уведомления» под системной диагностикой;
- действие использует ранее добавленный защищённый DELETE /api/admin/push/subscriptions;
- двойное подтверждение сохранено.

V9.1.3 MANDATORY PUSH ON LOGIN:
- после успешного PIN-входа проверяется действующая push-подписка устройства;
- если подписки нет, доступ в кабинет блокируется обязательным экраном подключения;
- при permission=default сразу вызывается системный запрос Notification.requestPermission();
- после разрешения автоматически создаётся/обновляется PushManager subscription и сохраняется на сервер;
- если permission=denied, сотруднику показывается инструкция по ручному включению разрешения;
- на iPhone без standalone/PWA показывается инструкция сначала добавить сайт на экран «Домой»;
- только после успешной регистрации push продолжается обычный welcome/cabinet flow;
- уже корректно подписанные устройства проходят без лишнего окна.

V9.2.0 ADAPTIVE ONBOARDING:
- при первом входе конкретного сотрудника на конкретном устройстве предлагается пошаговое обучение;
- обучение строится с учётом реальных прав сотрудника;
- обычный onboarding показывает главную, новости, задания, призы, команду, профиль и чаты, если они доступны;
- права сохраняются локально для employee_id на данном устройстве;
- при последующей выдаче нового права система сравнивает текущую capability map со старой;
- появляется отдельное обучение только по новым возможностям: новости, общие/индивидуальные задания, призы, конкурс, права и т.д.;
- во время шага сайт может автоматически открыть нужный раздел и подсветить кнопку «+»;
- в профиле добавлена кнопка «Пройти обучение заново»;
- обучение можно пропустить или отложить.

V9.2.1 TUTORIAL PROGRESS FIX:
- во время активного обучения повторная evaluateTutorialForCurrentUser блокируется;
- переход между разделами больше не перезапускает onboarding;
- progress/index сохраняется при renderEmp/renderUnifiedManager;
- предложение обучения и активное обучение имеют отдельные runtime locks;
- кнопки Далее/Назад работают только внутри активной tutorial session;
- после завершения/закрытия lock корректно снимается.

V9.2.2 TUTORIAL CHAT LAYER FIX:
- tutorialOverlay принудительно переносится последним прямым дочерним элементом body;
- overlay получил верхний безопасный z-index поверх мессенджера;
- карточка обучения всегда полностью непрозрачна и кликабельна;
- во время обучения #app не перехватывает клики;
- messenger stacking contexts больше не могут накрыть tutorial;
- подсветка элемента больше не использует огромный box-shadow, способный перекрывать карточку обучения.

V9.2.3 PASSIVE TUTORIAL:
- обучение больше не переключает вкладки и не открывает разделы;
- этапы не меняют umtab/etab и не вызывают renderEmp/renderUnifiedManager;
- на компьютере подсвечивается только соответствующая кнопка верхней навигации;
- «Управление» может быть подсвечено, но меню не раскрывается автоматически;
- кнопка «+» подсвечивается только если она уже видима на текущем экране;
- на мобильном, если вкладка скрыта, подсвечивается кнопка-гамбургер;
- текущий открытый раздел остаётся неизменным на протяжении обучения.

V9.2.4 STRICT PUSH GATE:
- push является обязательным условием доступа к кабинету сотрудника;
- проверка выполняется после PIN, при восстановлении сессии, focus/visibilitychange/online;
- проверяется не только Notification.permission, но и реальная PushManager subscription;
- существующая browser subscription на каждом входе повторно регистрируется в Neon, поэтому manager reset DB автоматически восстанавливается;
- если VAPID key изменён, старая подписка удаляется и создаётся новая;
- при permission=default сайт сразу пытается вызвать системный prompt;
- если браузер не разрешает автоматический prompt, кабинет остаётся заблокирован и пользователь обязан нажать «Разрешить уведомления»;
- при permission=denied кабинет остаётся заблокирован до ручного разрешения в настройках браузера;
- уведомления обязательны для обычных сотрудников, supervisor и сотрудника «Управляющий».

V9.2.5 PUSH KEY ROUTE FIX:
- добавлен отсутствовавший GET /api/push/key;
- маршрут доступен авторизованным employee/supervisor, включая сотрудника «Управляющий»;
- возвращает текущий VAPID_PUBLIC_KEY для PushManager.subscribe;
- если ключ не настроен, возвращается понятная 503 ошибка;
- push/key добавлен в smoke tests.

V9.3.0 FIRST LOGIN + PUSH:
- базовое обучение хранит факт первого предложения в Neon через onboarding_offered_at;
- базовое обучение предлагается ровно один раз для аккаунта сотрудника;
- второй, третий и дальнейшие входы базовое обучение не показывают, даже с другого устройства;
- onboarding_capabilities хранит уже обученные права;
- новые права запускают только соответствующее мини-обучение;
- Notification.requestPermission запускается непосредственно из клика по кнопке входа с PIN до первого await;
- после PIN требуется реальная PushManager subscription, зарегистрированная в Neon;
- без push кабинет не открывается.
